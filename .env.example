# Google Calendar API OAuth Configuration
# Get these from Google Cloud Console (https://console.cloud.google.com/)
# 1. Create a new project or select an existing one
# 2. Enable the Google Calendar API
# 3. Create OAuth 2.0 credentials (Web application)
# 4. Add authorized redirect URIs: http://localhost:5173/api/auth/callback (dev) and your production URL
# 5. Download the credentials JSON and extract these values

GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5173/api/auth/callback

# Session secret for encrypting OAuth tokens (generate a random string)
SESSION_SECRET=generate-a-random-secret-here

# Optional: Default calendar colors (comma-separated)
# Format: CALENDAR_ID:COLOR
# Colors: red, orange, yellow, green, blue, purple, pink, gray
CALENDAR_COLORS=primary:blue,#contacts@group.v.calendar.google.com:pink
